# Runtime dependencies (pinned ranges mirror pyproject.toml).
# For a fully reproducible build, generate a lockfile with `pip-compile` or `uv pip compile`.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
neo4j>=5.22,<6.0
psycopg[binary]>=3.2,<4.0
redis>=5.0,<6.0
celery>=5.4,<6.0
httpx>=0.27,<1.0
